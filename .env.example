# Bot User Configuration
# Each bot service requires a separate user account in Forgejo with appropriate permissions
# See BOT_USERS.md for detailed setup instructions

# RENOVATE_TOKEN: Personal Access Token for Renovate Bot user
# Create bot user: "renovate-bot"
# Required permissions: repo (read/write), write:package
# Create in Forgejo: Settings -> Applications -> Generate New Token
RENOVATE_TOKEN=your_renovate_bot_pat_here

# RENOVATE_GITHUB_TOKEN: Personal Access Token from GitHub
# Used to access GitHub for fetching release notes and changelogs
# Create this at: https://github.com/settings/tokens
# Required scopes: public_repo (or repo for private repos)
RENOVATE_GITHUB_TOKEN=your_github_pat_here

# AUDITLM_TOKEN: Personal Access Token for AuditLM Bot user
# Create bot user: "auditlm-bot"
# Required permissions: repo (read/write), write:issue
# Create in Forgejo: Settings -> Applications -> Generate New Token
AUDITLM_TOKEN=your_auditlm_bot_pat_here

# FORGEJO_MCP_TOKEN: Personal Access Token for Forgejo MCP Bot user
# Create bot user: "mcp-bot"
# Required permissions: repo (read), read:user, read:organization
# Create in Forgejo: Settings -> Applications -> Generate New Token
FORGEJO_MCP_TOKEN=your_mcp_bot_pat_here
